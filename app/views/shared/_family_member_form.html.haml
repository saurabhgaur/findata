= form_for(@family_member) do |f|
  .field
    = f.text_field :name, placeholder: "Family Member Name..."
    = f.text_field :email_id, placeholder: "Family Member EmailID..."
  = f.submit "Add Family Member", class: "btn btn-large btn-primary"