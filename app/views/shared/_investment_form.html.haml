= form_for(@investment) do |f|
  .field.form-inline
    = f.text_field :institution_name, placeholder: "Insitution Name..."
    = f.text_field :account_number, placeholder: "Account Number..."
    = f.submit "Add Investment", class: "btn btn btn-primary"